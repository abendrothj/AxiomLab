//! Top-level orchestrator that drives the agent loop.
//!
//! Skeleton – will be expanded with real LLM call / tool dispatch.

use tracing::info;

/// Run one iteration of the agent control loop.
pub async fn step() {
    info!("agent_runtime: orchestrator step (stub)");
}
