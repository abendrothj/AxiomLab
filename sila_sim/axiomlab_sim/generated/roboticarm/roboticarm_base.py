# Generated by sila2.code_generator; sila2.__version__: 0.14.0
from __future__ import annotations

from abc import ABC, abstractmethod
from typing import TYPE_CHECKING

from sila2.server import FeatureImplementationBase, MetadataDict

from .roboticarm_types import MoveArm_Responses

if TYPE_CHECKING:

    from ...server import Server


class RoboticArmBase(FeatureImplementationBase, ABC):
    parent_server: Server

    def __init__(self, parent_server: Server):
        """
        3-axis robotic arm for lab automation.
        """
        super().__init__(parent_server=parent_server)

    @abstractmethod
    def MoveArm(self, X: float, Y: float, Z: float, *, metadata: MetadataDict) -> MoveArm_Responses:
        """
        Move the arm to an absolute XYZ position in mm.


        :param X: Target X coordinate in millimetres (0–300).

        :param Y: Target Y coordinate in millimetres (0–300).

        :param Z: Target Z coordinate in millimetres (0–250).

        :param metadata: The SiLA Client Metadata attached to the call

        :return:

            - ReachedX: Confirmed X position after move.

            - ReachedY: Confirmed Y position after move.

            - ReachedZ: Confirmed Z position after move.


        """
