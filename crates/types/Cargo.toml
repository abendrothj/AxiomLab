[package]
name = "axiom-types"
description = "Shared domain types for AxiomLab — zero business logic."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
