//! Placeholder for pure-Rust FFT primitives.
//!
//! A full implementation will wrap a pure-Rust FFT crate (e.g., `rustfft`)
//! so that no C/Fortran dependency is introduced.

/// Marker module – real FFT ops will live here once `rustfft` is wired in.
pub fn placeholder() {}
